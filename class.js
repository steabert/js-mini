var someClass = baseClass.extend({
  member: 'something',
  other: a,
  someother: {
      better: b,
      yes: 'yes'
  },

  hello: function (there) {
      var something;
      return something;
  }
});

module.export = someclass;
